# Git HOL 5

## Objective
Clean up the local repository and synchronize it with the remote repository.

## Commands Executed

```bash
git status
git branch -a
git pull origin main
git push origin main
```

## Result

- Repository verified in a clean state.
- Remote repository synchronized successfully.
- All latest changes are available on GitHub.